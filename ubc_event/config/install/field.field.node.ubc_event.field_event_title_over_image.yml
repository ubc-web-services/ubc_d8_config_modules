langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_event_title_over_image
    - node.type.ubc_event
  # enforced:
  #   module:
  #     - ubc_event
id: node.ubc_event.field_event_title_over_image
field_name: field_event_title_over_image
entity_type: node
bundle: ubc_event
label: 'Display page title over image?'
description: 'By default, the page title displays above the content. If the event includes a feature image, and you would like the title to appear on top of it, select this option.'
required: false
translatable: false
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
