langcode: en
status: true
dependencies:
  config:
    - field.storage.paragraph.field_paragraph_reference
    - node.type.ubc_announcement
    - node.type.ubc_event
    - paragraphs.paragraphs_type.card_auto
id: paragraph.card_auto.field_paragraph_reference
field_name: field_paragraph_reference
entity_type: paragraph
bundle: card_auto
label: 'Existing Content'
description: 'Select a piece of existing content to embed it as a card. Begin typing the title of the card and then select it when you see the appropriate content become available. Note that this has limited options.'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      ubc_announcement: ubc_announcement
      ubc_event: ubc_event
    sort:
      field: title
      direction: ASC
    auto_create: false
    auto_create_bundle: ubc_announcement
field_type: entity_reference
