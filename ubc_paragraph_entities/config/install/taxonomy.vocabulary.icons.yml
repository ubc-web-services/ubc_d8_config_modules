langcode: en
status: true
dependencies: 
  enforced:
    module:
      - ubc_paragraph_entities
name: Icons
vid: icons
description: 'Maps svg ids loaded into the page to allow insertion via the use tag.'
weight: 0
